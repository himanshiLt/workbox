framework: mocha
tier: large
postMerge:
  pattern:
    - './test/all/node/test-yarn-installation.js'
preMerge:
  pattern:
    - './test/all/node/test-yarn-installation.js'
preRun:
  command:
    - npm install
postRun:
  command:
    - node --version
version: 2.0

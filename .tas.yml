framework: mocha
tier: xlarge
postMerge:
  pattern:
    - './test/all/node/test-exports.js'
preMerge:
  pattern:
    - './test/all/node/test-exports.js'
preRun:
  command:
    - npm install
postRun:
  command:
    - node --version
version: 2.0
